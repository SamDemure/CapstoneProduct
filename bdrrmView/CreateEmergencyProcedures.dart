import 'package:flutter/material.dart';

class CreateEmergencyProceduresPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Create Pre-Evacuation Alert'),
      ),
      body: Center(
        child: Text('Create Pre-Evacuation Alert Page'),
      ),
    );
  }
}
